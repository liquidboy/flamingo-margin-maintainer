apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: maintainer-btc
spec:
  serviceName: maintainer-btc
  replicas: 1
  selector:
    matchLabels:
      app: maintainer-btc
  template:
    metadata:
      labels:
        app: maintainer-btc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
        runAsGroup: 2000
      containers:
        - name: maintainer-btc
          image: maintainer-image
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "1Gi"
          envFrom:
            - secretRef:
                name: maintainer-secrets
          env:
            - name: DRY_RUN
              value: "false"
            - name: NODE_ENV
              value: "prod"
            - name: COLLATERAL_SCRIPT_HASH
              value: "0xd6abe115ecb75e1fa0b42f5e85934ce8c1ae2893"
            #- name: PRIVATE_KEY [comes from k8 secrets populated from local keepass via kse.sh]
            #  value: XXXXXXXXXXX
          ports:
            - containerPort: 50597
              name: http 
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 2000
